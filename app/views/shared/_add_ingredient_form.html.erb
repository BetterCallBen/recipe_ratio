<%= simple_form_for locales do |f| %>
  <%= f.input :quantity, label: "Quantité" %>
  <%= f.input :unity, label: "Unité" %>
  <%= f.association :ingredient %>
  <%= f.button :submit, value: "Ajouter" %>
<% end %>
