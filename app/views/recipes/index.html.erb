<div class="recipes">
  <% @recipes.each do |recipe| %>
    <%= link_to recipe_path(recipe) do %>
      <div class="recipe">
        <p>name: <%= recipe.name %></p>
        <p>for: <%= recipe.number_of_people %> people</p>
      </div>
    <% end %>
  <% end %>
</div>
